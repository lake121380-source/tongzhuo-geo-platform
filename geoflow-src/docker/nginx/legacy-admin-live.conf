# 旧 Blade 后台的**回滚态**：不做任何拦截，`/legacy-admin` 照常透传给 Laravel。
#
# 用途：退役拉闸（GEOFLOW_LEGACY_ADMIN_STATE=retired）后发现还有只存在于旧后台的操作路径时，
# 把它设回 `live` 并 `docker compose up -d web` 即可恢复，秒级。
#
# 这个文件刻意为空（只有注释）——nginx 允许 include 空文件，两态因此是「有拦截」与「无拦截」，
# 而不是往配置里塞条件分支。
